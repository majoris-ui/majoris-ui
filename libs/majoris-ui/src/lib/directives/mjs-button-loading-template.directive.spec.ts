import { ButtonLoadingTemplateDirective } from './mjs-button-loading-template.directive';

describe('MjsButtonLoadingTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonLoadingTemplateDirective();
    expect(directive).toBeTruthy();
  });
});

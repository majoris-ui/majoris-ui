import { MjsContrastTextColorDirective } from './mjs-contrast-text-color.directive';

describe('MjsContrastTextColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MjsContrastTextColorDirective();
    expect(directive).toBeTruthy();
  });
});
